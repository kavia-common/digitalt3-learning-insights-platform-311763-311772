# digitalt3-learning-insights-platform-311763-311772

## Dependency updates (lms_backend)

- `dotenv` upgraded to `^17.2.3`
- `cors` upgraded to `^2.8.6`
- Note: `express` has a newer major (`v5`), but this backend remains on `express@^4` to avoid breaking changes.